[package]
name = "lumi_ast"
version = "0.1.0"
edition = "2021"
description = "Abstract Syntaxt Tree (AST) for Lumi engine"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
serde_json = "1.0" 