--------------------------------------------------------------------------

    r#"
        fn test(a) { 
            return test(a - 1);
        }
    "#

    this results in error: Undeclared variable 'test'

-----------------------------------------------------------------------------
